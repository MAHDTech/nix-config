# Config

This config is broken up into the following files:

| Folder    | Description                                |
| --------- | ------------------------------------------ |
| `data`    | Items that collect data                    |
| `widgets` | Items that display data                    |
| `styles`  | CSS styles for the Widgets                 |
| `types`   | TypeScript types generated by `ags --init` |
