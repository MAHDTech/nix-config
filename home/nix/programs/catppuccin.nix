{
  catppuccin = {
    enable = true;

    accent = "mauve";
    flavor = "mocha";

    bottom = {
      enable = true;
    };

    btop = {
      enable = true;
    };

    cursors = {
      enable = true;
    };

    delta = {
      enable = false;
    };

    dunst = {
      enable = true;
    };

    fzf = {
      enable = true;
    };

    gtk = {
      enable = true;
      gnomeShellTheme = false;
    };

    hyprland = {
      enable = true;
    };

    hyprlock = {
      enable = true;
    };

    mako = {
      enable = false;
    };

    mpv = {
      enable = true;
    };

    nvim = {
      enable = true;
    };

    obs = {
      enable = true;
    };

    starship = {
      enable = true;
    };
  };
}
