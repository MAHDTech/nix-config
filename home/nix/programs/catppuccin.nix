{
  catppuccin = {
    enable = true;

    accent = "mauve";
    flavor = "mocha";

    kvantum = {
      enable = false;
    };

    gtk = {
      icon = {
        enable = false;
      };
    };

    bottom = {
      enable = true;
    };

    btop = {
      enable = true;
    };

    cursors = {
      enable = false;
    };

    delta = {
      enable = false;
    };

    dunst = {
      enable = true;
    };

    fzf = {
      enable = true;
    };

    hyprland = {
      enable = true;
    };

    hyprlock = {
      enable = true;
    };

    mako = {
      enable = false;
    };

    mpv = {
      enable = true;
    };

    nvim = {
      enable = true;
    };

    obs = {
      enable = true;
    };

    starship = {
      enable = false;
    };
  };
}
