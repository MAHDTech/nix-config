{
  imports = [
    # Common tooling across all projects
    ./tools.nix

    # Devenv and Cachix
    ./devenv.nix

    # Games
    #./games.nix

    # Moved to devenv
    #./ansible.nix

    # Moved to devenv.
    #./cloud.nix

    # Moved to devenv.
    #./nix.nix

    # Moved to devenv
    #./cli.nix

    # Moved to devenv.
    #./development.nix

    # Moved to devenv.
    #./gui.nix
  ];
}
